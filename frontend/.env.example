# API Base URL
# - In Dev: Use '/api' to leverage Vite Proxy (targets localhost:8000)
# - In Prod: Use '/api' to leverage Nginx Proxy (targets backend container)
VITE_API_BASE_URL=/api

# Application Auth Token (Demo Only)
# In a real app, this might come from a login flow.
VITE_AUTH_TOKEN=
